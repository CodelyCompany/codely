kind: Service
apiVersion: v1
metadata:
    name: backend_containers-service
    namespace: production
spec:
    type: ClusterIP
    ports:
        - port: 5001
          targetPort: 5001
          protocol: TCP
    selector:
        app: codely
        type: backend_containers
