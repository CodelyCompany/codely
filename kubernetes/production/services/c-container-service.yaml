kind: Service
apiVersion: v1
metadata:
    name: c-container-service
    namespace: production
spec:
    type: ClusterIP
    ports:
        - port: 6006
          targetPort: 6006
          protocol: TCP
    selector:
        app: codely
        type: c-container
